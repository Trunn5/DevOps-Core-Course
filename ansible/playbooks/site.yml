---
- name: Full site deployment
  hosts: webservers
  become: true
  vars_files:
    - ../group_vars/all.yml

  roles:
    - role: common
      tags: [common]
    - role: docker
      tags: [docker]
    - role: web_app
      tags: [app_deploy]
